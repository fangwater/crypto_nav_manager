[Unit]
Description=Run hourly intra latency ingest at the top of each UTC hour

[Timer]
OnCalendar=hourly
Persistent=true
AccuracySec=1s
Unit=crypto-intra-hourly-latency.service

[Install]
WantedBy=timers.target
